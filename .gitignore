# Root-level ignore (each app also has its own .gitignore)
node_modules
.DS_Store
.env
.env.*
!.env.example

# Client raw assets / working files — not part of the deployed app
/Blackstone Chauffeur/
/branding/
/PRODUCTION_READINESS_REPORT.md

# Scratch folder used to relocate stray build-output copies out of the
# way — never part of the app itself.
/_trash/
